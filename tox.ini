[tox]
minversion = 3.4.0


[testenv]
deps =
    -rrequirements.txt
    -rdev-requirements.txt
commands = pytest -vs --cov flaskdoc --cov-report xml {posargs}

[testenv:coverage]
passenv = CODACY_PROJECT_TOKEN
deps =
    -rdev-requirements.txt
commands = python-codacy-coverage -r coverage.xml