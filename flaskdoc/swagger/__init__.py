from swagger.models import License, Contact, Info, Server, ServerVariable, Component, Paths, PathItem, Operation, GET, \
    POST, PUT, HEAD, OPTIONS, PATCH, TRACE, HttpMethod, ParameterLocation, Style, Parameter, PathParameter, \
    QueryParameter, HeaderParameter, CookieParameter, Tag
from swagger.core import OpenApi
